spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/reelz}
    username: ${DATABASE_USERNAME:reelz}
    password: ${DATABASE_PASSWORD:reelz}
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

